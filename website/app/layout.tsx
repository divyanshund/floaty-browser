import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Floaty Browser - Float Above Everything',
  description: 'A beautiful macOS browser that lets you browse the web in floating bubbles that stay visible while you work.',
  keywords: 'macOS browser, floating browser, bubble browser, productivity, web browser',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  )
}

